class ImageTimer {
    constructor() {
        this.artistImages = this.loadArtistImages();
    }

    loadArtistImages(eleList) {
        let artistImages = {};
        for (let i = 0; i < eleList.length; i++) {
            // eleList.innerHTML
        }
    }
}

module.exports = ImageTimer;